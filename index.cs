using System;
/*
 * https://docs.microsoft.com/en-us/dotnet/api/system?view=net-5.0
 */
using System.Net;
/*
 * Mail, IP Address & Location, Security, etc.
 * https://docs.microsoft.com/en-us/dotnet/api/system.net?view=net-5.0
 */

namespace NetwireMain
{
	class History
	{
		void Save(what)
		{
		}
		string Retrieve(what)
		{
		}
	}
	class Networking
	{
		string Address() // Retrieve user IP address
		{
		}
		void Mail() // Sending the user a notification
		{
		}
	}
	class Setting
	{
		string Detect() // Detects user's OS
		{
		}
		void Timer() // Asynchronous pinger to wait for a network connection
		{
		}
		void Main() // STARTS HERE
		{
		}
	}
}
